Установите зависимости:

    ```bash
    npm install
    ```

    Или, если вы используете Yarn:

    ```bash
    yarn install
    ```

## Запуск проекта

### Режим разработки

Чтобы запустить проект в режиме разработки:

```bash
npm run dev
